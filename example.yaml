example: | = de example
  Set(varGreeting, "Hello World!");
  Set(varResult, Text(varGreeting));

example_newline:
  = powerfx
  // PowerFx code example
  Set(varGreeting, "Hello World!");
  Set(varResult, Text(varGreeting));
